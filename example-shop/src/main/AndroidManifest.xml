<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".ExampleShopApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">

        <activity
            android:name=".settings.SettingsActivity"
            android:exported="true"
            android:theme="@style/CheckoutTheme.Toolbar"
            android:windowSoftInputMode="stateHidden">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".checkout.CheckoutActivity"
            android:exported="true"
            android:theme="@style/CheckoutTheme.CollapsingToolbar"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <activity
            android:name=".summary.SummaryActivity"
            android:exported="true"
            android:theme="@style/CheckoutTheme.CollapsingToolbar" />

        <activity
            android:name=".confirm.ConfirmActivity"
            android:exported="true"
            android:noHistory="true"
            android:theme="@style/CheckoutTheme.NoToolbar" />

        <activity
            android:name="com.payoneer.checkout.redirect.PaymentRedirectActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/CheckoutTheme.NoToolbar">

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <data android:scheme="${applicationId}.mobileredirect" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>
        </activity>

    </application>
</manifest>
